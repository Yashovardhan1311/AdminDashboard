{
  "name": "custom-admin-dashboard",
  "version": "1.0.0",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "start": "npx babel --watch src --out-dir . --presets react-app/prod"
  }
}
